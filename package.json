{
  "name": "student-productivity-board",
  "version": "1.0.0",
  "description": "A modern Kanban board with Firebase backend for student task management",
  "main": "index.html",
  "scripts": {
    "start": "node index.html",
    "install": "npm install",
    "deploy": "firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase": "^9.0.0"
  },
  "devDependencies": {
    "firebase-admin": "^11.0.0",
    "firebase": "^9.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
